# 프로젝트 작업 지침

## 키움 REST API

- 키움 REST API를 구현할 때는 `kra-docs` 스킬로 관련 TR 명세를 확인한다.
- 문서에 없는 파라미터, 응답 필드 또는 동작을 추측하지 않는다.

## 환경변수와 비밀정보

- `.env`는 환경변수 이름과 구성을 확인하기 위해 읽을 수 있다.
- `.env`의 실제 값은 응답, 로그, 오류 메시지, 테스트 결과 또는 클라이언트 코드에 노출하지 않는다.
- `.env`를 임의로 수정하거나 삭제하지 않으며, 저장소에 커밋하지 않는다.
- 앱 키와 시크릿은 서버에서만 사용한다.

## 투자 환경과 주문 안전

- 실투자, 국내 모의투자, 해외 모의투자 환경을 구분하고, 선택된 환경에 맞는 인증정보와 API 도메인을 사용한다.
- 개발이나 테스트 과정에서 실투자 주문은 절대 실행하지 않는다.
- 모의투자 주문은 자유롭게 실행할 수 있다.
